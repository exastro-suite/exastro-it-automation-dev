# トークン発行

APIを呼び出す際に必要なアクセストークンの発行には、リフレッシュトークンが必要です。ログインユーザーのアイコンメニューから「トークン発行」を選択し、トークンの発行・一覧表示を行います。

- 二要素認証を使用している場合、トークン発行時に認証アプリのワンタイムパスワードの入力が必要です。
- 発行されたトークンは再表示できないため、発行結果画面で必ず保存する必要があります。
- 発行済みトークンの有効期限はトークン一覧で確認できます。
- 不要なトークンは削除できますが、**削除すると全てのトークンが無効になります**（個別削除ではなく全トークン無効化）。
